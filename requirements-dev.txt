# Development dependencies for prs-zigbee2mqtt-connector.
# Runtime connector dependency (base library for Peresvet connectors).
prs-connector-core>=0.13.0

# Dev / test tooling (aligned with mp-co-ru/prs-connector-core conventions).
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-mock>=3.15.0
ruff>=0.9.0
